{
  "name": "debtctl-example",
  "version": "0.0.0",
  "private": true,
  "description": "Runnable example showing debtctl governing npm overrides via a .debtctl.json sidecar.",
  "packageManager": "npm@10.8.0",
  "scripts": {
    "debt:check": "debtctl check"
  },
  "dependencies": {
    "ms": "^2.1.3"
  },
  "overrides": {
    "ms": "^2.1.3",
    "bar": "1.2.3",
    "baz": "^5.0.0"
  },
  "devDependencies": {
    "debtctl": "file:.."
  }
}
